web: gunicorn app:app

#First app = filename (app.py)
#Second app = Flask object name (app = Flask(__name__))

# -- "This looks like Python… but gunicorn is not a Python variable."
#But Procfile is NOT Python.

#It is just a deployment instruction file.

#So:

#👉 That red underline does NOT matter.
#👉 It will NOT break anything.
#👉 You can safely ignore it.